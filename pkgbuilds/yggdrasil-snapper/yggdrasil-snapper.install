#!/hint/bash

post_install() {
	install -D /etc/conf.d/snapper /etc/yggdrasil/snapper/backup/snapper
	sed -Ei 's/^(SNAPPER_CONFIGS=).*/\1"root home"/' /etc/conf.d/snapper

	systemctl enable snapper-boot.timer
	systemctl enable snapper-timeline.timer
	systemctl enable snapper-cleanup.timer
}
